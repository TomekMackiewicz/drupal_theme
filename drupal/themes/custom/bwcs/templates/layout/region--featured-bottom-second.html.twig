{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
    'mx-auto',
    'justify-content-center'
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}